<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.llu.mapper.UserMapper">

    <select id="selectUser" resultType="com.llu.bean.User"  >
        SELECT * FROM user WHERE id = #{id}
    </select>
    <select id="selectAllUser" resultType="com.llu.bean.User">
        select * from user order by ${orderBy}
    </select>
    <select id="selectAllUserTestLike" resultType="com.llu.bean.User">
        select * from user where userName like '%${userName}%'
    </select>
    <select id="selectAllUserTestIn" parameterType="list" resultType="com.llu.bean.User">
        select * from user where userName in (${inName})
    </select>

</mapper>



